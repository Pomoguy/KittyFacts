setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    POSTGRES_USER: camunda
    POSTGRES_PASSWORD: 1511
    POSTGRES_DB: camunda

build:
  docker:
    web: Dockerfile
  config:
    SPRING_PROFILE: prod
